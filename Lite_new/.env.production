VITE_API_BASE_URL=https://shreenika-ai-backend-507468019722.asia-south1.run.app
doesn't iHowever, this VITE_API_BASE_URL on Cloud Run won't actually work for your frontend. Vite variables are baked in at build time, not runtime. Setting it as a Cloud Run env var has no effect — nginx just serves static files and doesn't inject env vars.

The .env.production file you just added to GitHub is what actually sets it during the build. So this Cloud Run variable is harmless but useless — you can leave it or remove it, doesn't matter.